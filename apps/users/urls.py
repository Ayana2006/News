from django.urls import path
# from apps.users.views import ParentsAPIView, RegisterAPIView

urlpatterns = [
    # path('parents/', ParentsAPIView.as_view()),
    # path('register/', RegisterAPIView.as_view()),
    
]